# Network Scraper для сбора данных с Яндекс Карт

Этот модуль предоставляет более эффективный способ сбора данных о компьютерных клубах путем перехвата и парсинга JSON-ответов из network-запросов Яндекс Карт.

## Преимущества подхода

1. **Более быстрый сбор данных** - нет необходимости парсить DOM-элементы
2. **Больше информации** - JSON содержит дополнительные данные (координаты, рейтинг, телефоны и т.д.)
3. **Меньше ошибок** - стабильная структура JSON vs изменчивые CSS-селекторы
4. **Автоматическое удаление дубликатов** - встроенная фильтрация повторных записей

## Архитектура

### Основные компоненты:

1. **`NetworkClubsScraper`** - основной класс для перехвата network-запросов
2. **`NetworkClubsParser`** - интерфейс для создания парсеров JSON-данных
3. **`YandexMapsParser`** - готовая реализация парсера для Яндекс Карт

## Использование

### Быстрый старт

```bash
# Сбор данных по всем регионам
npm run network-scraper
npm run network-scraper global

# Сбор данных по конкретному региону (например, Орёл - ID 10)
npm run network-scraper 10

# Список доступных регионов
npm run list-regions

# Быстрый тест региона (альтернативный способ)
npm run test-region 10
npm run test-region list
```

### Программное использование

```typescript
import { runNetworkClubsScraping } from './network-clubs-scraper';
import { yandexMapsParser } from './yandex-maps-parser';

// Запуск с 20 прокрутками
const result = await runNetworkClubsScraping(yandexMapsParser, 20);
console.log(`Собрано ${result.totalFound} клубов`);
```

### Создание собственного парсера

```typescript
import { NetworkClubsParser, ClubData } from './network-clubs-scraper';

class MyCustomParser implements NetworkClubsParser {
  isTargetRequest(url: string, method: string): boolean {
    // Логика определения нужных запросов
    return url.includes('/api/search') && method === 'GET';
  }

  parseClubsData(jsonData: any): ClubData[] {
    // Логика извлечения данных о клубах из JSON
    return jsonData.items.map(item => ({
      name: item.title,
      address: item.location.address,
      phone: item.contact?.phone,
      // ... другие поля
    }));
  }
}

// Использование
const customParser = new MyCustomParser();
const result = await runNetworkClubsScraping(customParser, 15);
```

## Настройка парсера

Вам нужно будет адаптировать `YandexMapsParser` под актуальную структуру JSON, которую возвращают Яндекс Карты:

1. Откройте Developer Tools в браузере
2. Перейдите на вкладку Network
3. Найдите запросы, содержащие данные о клубах
4. Изучите структуру JSON в ответах
5. Обновите методы в `yandex-maps-parser.ts`:
   - `isTargetRequest()` - для фильтрации нужных URL
   - `parseClubsData()` - для извлечения данных

## Структура данных

### ClubData
```typescript
interface ClubData {
  name: string;           // Название клуба
  address: string;        // Адрес
  coordinates?: {         // Координаты
    lat: number;
    lon: number;
  };
  phone?: string;         // Телефон
  website?: string;       // Веб-сайт
  rating?: number;        // Рейтинг
  reviews?: number;       // Количество отзывов
  workingHours?: string;  // Часы работы
  rawData?: any;          // Исходные данные JSON
}
```

### ScrapingResult
```typescript
interface ScrapingResult {
  totalFound: number;     // Количество найденных клубов
  clubs: ClubData[];      // Массив клубов
  timestamp: string;      // Время сбора
  sourceUrl: string;      // URL источника
  networkRequests: number; // Количество перехваченных запросов
}
```

## Отладка

1. **Raw данные** - все JSON ответы сохраняются в `results/debug/`
2. **Логирование** - подробные логи процесса перехвата и парсинга
3. **rawData поле** - исходные JSON данные сохраняются в каждом ClubData

## Умная логика скролла

Скрипт использует интеллектуальную систему скролла с отслеживанием прогресса:

1. **Обычный скролл**: Прокручивает список результатов в контейнере `.scroll__container`

2. **Отслеживание прогресса**: 
   - Сравнивает количество найденных клубов между итерациями
   - **Умное исключение дубликатов на лету**:
     - По названию + адресу (регистронезависимо)
     - По координатам (с точностью до 6 знаков)
     - Обработка в реальном времени при получении JSON
   - Останавливается если количество не изменилось 2 итерации подряд

3. **Обнаружение конца списка**: При обнаружении элемента `.add-business-view` (означает конец результатов)

4. **Переключение на карту**: Если `.add-business-view` виден более 1 секунды:
   - Наводит мышку на центр карты (`.map-container`)
   - Выполняет двойной скролл **назад** на карте для изменения области просмотра
   - Возвращает фокус на список результатов
   - Продолжает сбор данных с новой области

5. **Завершение**: Останавливается если:
   - Количество клубов не изменялось 2 итерации подряд
   - Скролл по карте не помог загрузить новые результаты
   - Достигнуто максимальное количество итераций

### Визуальные индикаторы в консоли:
- `🧹 Очищены данные предыдущего сбора` - инициализация
- `📜 Прокручиваем для загрузки данных...` - начало скролла
- `📦 Извлечено новых клубов из запроса: 5 (всего было: 8)` - новые клубы после фильтрации
- `🔄 Все клубы из запроса уже были найдены ранее (8 дубликатов)` - все дубликаты
- `🔄 Пропускаем дубликат по названию+адресу: КиберПрайд` - конкретный дубликат
- `🔄 Пропускаем дубликат по координатам: КиберПрайд (36.058950,52.971922)` - дубликат по координатам
- `📈 Прогресс: +5 клубов (всего: 23)` - успешное добавление новых клубов
- `⚠️ Количество клубов не изменилось (1/2): 23` - предупреждение о застое
- `🛑 Количество клубов не изменялось 2 итерации подряд, завершаем сбор` - автоостановка
- `⏸️ Обнаружен элемент add-business-view, ждем 1 секунду...` - обнаружен конец списка
- `🗺️ Элемент add-business-view виден больше 1 секунды, переключаемся на карту...` - переход к карте
- `🖱️ Наводим мышку на карту...` - работа с картой
- `🔄 Выполняем скролл назад на карте для изменения области просмотра...` - скролл карты назад
- `↩️ Возвращаем фокус на список результатов...` - возврат к списку
- `🏁 Завершение скролла. Итого собрано клубов: 47` - итоговая статистика

## Выходные файлы

### Для всех регионов:
- `results/network-clubs-data-all-regions.json` - полные данные в JSON
- `results/network-clubs-data-all-regions.csv` - данные в CSV для Excel

### Для конкретного региона:
- `results/network-clubs-data-region-{ID}.json` - данные региона в JSON
- `results/network-clubs-data-region-{ID}.csv` - данные региона в CSV

### Отладочные файлы:
- `results/debug/network-*.json` - raw JSON ответы для отладки

Файлы CSV включают дополнительные колонки:
- **ID региона** - числовой идентификатор региона
- **Название региона** - человекочитаемое название региона

## Режимы работы

### 1. Сбор по всем регионам
```bash
npm run network-scraper global   # По всем регионам
```
Обрабатывает все регионы из файла `results/regions-simple.json` с паузами между регионами.

### 2. Сбор по конкретному региону
```bash
npm run network-scraper 10       # Только регион с ID 10 (Орёл)
npm run network-scraper 1        # Только Москва
npm run network-scraper 43       # Только Казань
```
Обрабатывает только указанный регион для быстрого тестирования.

### 3. Вспомогательные команды
```bash
npm run list-regions             # Показать все доступные регионы
npm run test-region 10           # Альтернативный способ тестирования региона
npm run test-region list         # Показать регионы через test-region
```

## Параметры запуска

- **scrollIterations** - количество прокруток (по умолчанию 15)
- **headless** - режим браузера (по умолчанию false для визуального контроля)
- **regionId** - ID конкретного региона для тестирования

## Расширение функциональности

Для добавления новых полей:

1. Обновите интерфейс `ClubData`
2. Добавьте метод извлечения в `YandexMapsParser`
3. Обновите CSV-генератор при необходимости 